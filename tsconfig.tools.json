{
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    "lib": ["ES2023", "DOM"],
    // `scripts/screenshots.ts` evaluates callbacks inside the browser via Playwright,
    // so the tooling project needs DOM types even though it runs under Node.
    "types": ["node"],
    "outDir": ".tsbuild/tools"
  },
  "include": [
    "scripts/**/*.ts",
    "tests/**/*.ts",
    "shared/**/*.ts",
    "content/**/*.ts",
    "vitest.config.ts"
  ]
}
